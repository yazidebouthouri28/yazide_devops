pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                sh 'echo Building...'
                sh 'mvn clean package'
            }
        }

        stage('Deploy') {
            when {
                branch "main"
            }
            steps {
                sh 'echo Deploying to server...'
                sh 'scp target/app.jar user@server:/opt/app/'
            }
        }

    }
}
